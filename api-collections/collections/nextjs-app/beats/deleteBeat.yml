info:
  name: Beats - Delete Beat
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{url}}/api/beats/midnight-bounce"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
