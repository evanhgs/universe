info:
  name: Beats - List Published
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}/api/beats"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
