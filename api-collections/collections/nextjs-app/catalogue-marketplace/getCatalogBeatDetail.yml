info:
  name: Catalogue Marketplace - Get Beat Detail
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/api/beats/{{beatSlug}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
