info:
  name: Marketplace - List Sales History
  type: http
  seq: 8

http:
  method: GET
  url: "{{url}}/api/marketplace/sales"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
