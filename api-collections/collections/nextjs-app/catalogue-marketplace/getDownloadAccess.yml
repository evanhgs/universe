info:
  name: Marketplace - Get Download Access
  type: http
  seq: 9

http:
  method: GET
  url: "{{url}}/api/marketplace/downloads/{{entitlementId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
