info:
  name: Marketplace - List Purchase History
  type: http
  seq: 7

http:
  method: GET
  url: "{{url}}/api/marketplace/purchases"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
