info:
  name: Catalogue Marketplace - Get Beat Preview Audio
  type: http
  seq: 3

http:
  method: GET
  url: "{{url}}/api/beats/{{beatSlug}}/preview"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
