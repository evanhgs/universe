info:
  name: Clerk - Sign In (Email Code Start)
  type: http
  seq: 2

http:
  method: POST
  url: "{{clerkFrontendApiUrl}}/v1/client/sign_ins"
  headers:
    - name: Content-Type
      value: application/json
    - name: Accept
      value: application/json
    - name: Origin
      value: "{{url}}"
  body:
    type: json
    data: |-
      {
        "identifier": "{{clerkIdentifier}}",
        "strategy": "email_code"
      }
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
