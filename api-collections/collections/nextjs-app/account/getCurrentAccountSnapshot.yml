info:
  name: Account - Get Current Snapshot
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}/api/account/me"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
